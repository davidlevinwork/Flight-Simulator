<UserControl x:Class="SimolatorDesktopApp_1.View.controls.AlgoritemDetect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimolatorDesktopApp_1.View.controls"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="250">
    <Grid>
        <ListView x:Name="ListViewAlgorithms" ItemsSource="{Binding Path=VM_AddToMyList}" HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="112" SelectionChanged="ListViewAlgoritems_SelectionChanged">
            <ListView.View>
                <GridView x:Name="GridViewFeatures">
                    <GridViewColumn Header="Algorithms" Width="170"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView x:Name="ListViewAnomalies" ItemsSource="{Binding Path=VM_AddAnomaliesToMyList}" HorizontalAlignment="Left" Height="100" Margin="117,0,-0.4,0" VerticalAlignment="Top" Width="133" SelectionChanged="ListViewAnomalies_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
